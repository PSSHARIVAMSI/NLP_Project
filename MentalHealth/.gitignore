.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
*.egg-info/

# Trained model weights are large binaries — regenerate via the notebook,
# or track with Git LFS / cloud storage instead of plain git.
models/**/*.pt
models/**/*.joblib
models/**/*.safetensors

# Local Streamlit / Claude settings
.streamlit/secrets.toml
.claude/settings.local.json
